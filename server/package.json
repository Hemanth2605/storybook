{
  "name": "component-request-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Node/Express backend that emails component requests with working Approve/Reject buttons.",
  "scripts": {
    "dev": "node --watch index.js",
    "start": "node index.js",
    "worker": "node worker.js",
    "worker:once": "node worker.js --once"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "nodemailer": "^9.0.3"
  }
}
